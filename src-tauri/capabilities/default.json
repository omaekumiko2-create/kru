{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the local KRU management window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
